<Window x:Class="AionDpsMeter.UI.Services.Windowing.BlazorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:blazor="clr-namespace:Microsoft.AspNetCore.Components.WebView.Wpf;assembly=Microsoft.AspNetCore.Components.WebView.Wpf"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ResizeMode="NoResize"
        >

    <!-- No RootComponents here on purpose: the page type is only known at
         runtime (passed into the constructor), so it's added in code-behind
         right after InitializeComponent() — same spot SettingsWindow.xaml.cs
         already does it, just parameterized instead of hardcoded. -->
    <blazor:BlazorWebView x:Name="BlazorWebView" HostPage="wwwroot/index.html" Background="Transparent" />
</Window>
